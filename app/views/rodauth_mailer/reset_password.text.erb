<%= t('mailers.password_reset.greeting', default: 'Reset your password') %>

<%= t('mailers.password_reset.intro', default: 'We received a request to reset the password for your account.') %>
<%= t('mailers.password_reset.instructions', default: 'Click the link below to set a new password.') %>

<%= @rodauth.reset_password_email_link %>

<%= t('mailers.password_reset.ignore', default: 'If you did not request this, you can safely ignore this email.') %>
